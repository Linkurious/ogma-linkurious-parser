import { Valcheck } from './Valcheck';
export declare class ValcheckNode<E> extends Valcheck<E> {
    /**
     * Check if the given file path points to a readable file.
     *
     * @param {string} key
     * @param {*} value A file path
     * @param {string} [rootPath] Will resolve `value` in `rootPath` instead of current working directory.
     * @returns {*} error, if any
     */
    file(key: string, value: unknown, rootPath?: string): E | void;
    /**
     * Check if `value` is a directory.
     *
     * @param {string} key
     * @param {*} value
     * @param {string} [rootPath] Will resolve `value` in `rootPath` instead of current working directory.
     * @returns {*} error, if any
     */
    dir(key: string, value: unknown, rootPath?: string): E | void;
}
